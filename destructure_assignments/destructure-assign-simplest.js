//destructure-assign-simplest.js
const person = { name: 'Alice', age: 30 };

// Unpacking properties into variables with matching names
const { name, age } = person;

console.log(name); // Output: Alice
console.log(age);  // Output: 30